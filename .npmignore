# 排除 TypeScript 源码（npm 包只发布编译产物）
src/

# 排除开发配置
tsconfig.json
.eslintrc*
.eslintignore
jest.config*

# 排除测试
__tests__/
*.test.ts
*.test.tsx
*.spec.ts
*.spec.tsx

# 排除 CI / 开发脚本
.github/
.husky/

# 排除 macOS 元数据
.DS_Store

# 排除日志
npm-debug.log*
yarn-debug.log*

# 保留（不要在此重复 files 字段已包含的内容）
# lib/      ← 由 package.json files 字段控制，会被打包
# README.md ← 同上
